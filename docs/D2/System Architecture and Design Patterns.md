# 🚀 Workshop Enrollment System Architecture

เอกสารฉบับนี้อธิบายถึงสถาปัตยกรรมซอฟต์แวร์ การเลือกใช้เทคโนโลยี และแนวคิดการออกแบบ (Design Patterns) ที่ใช้ในการพัฒนาระบบจัดการการสมัครเข้าอบรม

---

## 🏗️ 1. Architecture Layers (N-Tier Architecture)

ระบบถูกออกแบบโดยใช้สถาปัตยกรรมแบบ **Layered Architecture** เพื่อแยกส่วนรับผิดชอบออกจากกันอย่างชัดเจน (Separation of Concerns)



### 🔹 ชั้นที่ 1: Presentation Layer
* **Frontend (React):** จัดการ UI/UX รับข้อมูลจากผู้ใช้และส่ง Request
* **Backend (Controller):** ด่านหน้ารับ HTTP Request, ทำ Input Validation และส่ง Response
* **Components:** `LoginPage.jsx`, `authController.js`, `authMiddleware.js`
* **⚠️ Rules:** ห้ามเขียน Business Logic หรือ SQL Query ในชั้นนี้

### 🔹 ชั้นที่ 2: Business Logic Layer (The Brain)
* **หน้าที่:** ประมวลผลตามกฎธุรกิจ (เช่น ตรวจสอบโควตาที่นั่ง, เช็คการสมัครซ้ำ, Hashing Password)
* **Components:** `authService.js`, `workshopService.js`, `passwordUtils.js`
* **✅ Rules:** ต้องเป็น Pure Logic ไม่ขึ้นกับ Framework และไม่ดึงข้อมูลจาก DB โดยตรง (ต้องผ่าน Repository)

### 🔹 ชั้นที่ 3: Data Access Layer
* **หน้าที่:** สื่อสารกับ MySQL โดยตรง จัดการ CRUD Operations และแปลงข้อมูลเป็น Object
* **Components:** `UserModel.js`, `WorkshopModel.js`, `db.config.js`
* **✅ Rules:** ทำหน้าที่ดึง-เก็บ-แก้ไขข้อมูลเท่านั้น ไม่มีการตัดสินใจทางธุรกิจในชั้นนี้

---

## 🔄 2. Data Flow (การไหลของข้อมูล)

เมื่อมีการกดปุ่ม **"สมัครอบรม"** ข้อมูลจะเดินทางดังนี้:

1.  **Client:** ส่ง `POST /enroll` พร้อมข้อมูล User & Workshop
2.  **Controller:** ตรวจสอบความครบถ้วนของข้อมูล (Input Check)
3.  **Service:** ตรวจสอบเงื่อนไข "ที่นั่งว่างไหม?" และ "สมัครไปหรือยัง?"
4.  **Repository:** รันคำสั่ง SQL เพื่อ `INSERT` ข้อมูลการสมัคร และ `UPDATE` จำนวนที่นั่ง
5.  **Database:** บันทึกข้อมูลจริงและส่งผลลัพธ์กลับขึ้นไปจนถึงหน้าจอผู้ใช้

---

## 🛠️ 3. Technology Stack

| Technology | Role | Justification |
| :--- | :--- | :--- |
| **React.js + Tailwind** | Frontend | Component-based พัฒนาง่าย และ UI สวยงามเป็นมาตรฐาน |
| **Node.js + Express** | Backend | ใช้ภาษา JavaScript ทั้งระบบ (Full-stack JS) ลด Learning Curve |
| **MySQL** | Database | จัดการ Relational Data ได้ดีเยี่ยม รักษาความถูกต้องของข้อมูล (ACID) |
| **JWT** | Auth | มาตรฐาน Stateless Authentication ปลอดภัยและ Scale ง่าย |

---

## 🎨 4. Design Patterns Selection

### 1️⃣ MVC Pattern (Model-View-Controller)
ใช้จัดการโครงสร้างหลักของ Backend เพื่อแยกส่วนข้อมูล (Model), ส่วนควบคุม (Controller) และส่วนแสดงผล (JSON Response)
* **Benefit:** ช่วยให้ทีมแบ่งงานกันทำได้ชัดเจน และง่ายต่อการเขียน Unit Test

### 2️⃣ Singleton Pattern
ใช้กับการเชื่อมต่อฐานข้อมูล (**Database Connection Pool**)
* **Benefit:** ประหยัดทรัพยากรเครื่อง โดยการเปิด Connection เพียงตัวเดียวตลอดการทำงาน ป้องกันปัญหา Memory Leak

### 3️⃣ Higher-Order Component (HOC)
ใช้จัดการ **Protected Routes** บน Frontend
* **Benefit:** ลดความซ้ำซ้อนของโค้ด (DRY - Don't Repeat Yourself) โดยการสร้าง Component กลางมาหุ้มหน้า Dashboard เพื่อเช็คสิทธิ์ก่อนเข้าถึง

### 4️⃣ Service Layer Pattern
ใช้แยก Business Logic ออกจาก Controller
* **Benefit:** ทำให้ Controller มีหน้าที่แค่รับ-ส่งข้อมูล ส่วนการคำนวณที่ซับซ้อนจะรวมศูนย์อยู่ที่ Service Layer ทำให้ Code สะอาดและอ่านง่าย (Clean Code)

---

> **Note:** การออกแบบนี้เน้นที่ความยืดหยุ่น (Scalability) และการบำรุงรักษา (Maintainability) ในระยะยาว